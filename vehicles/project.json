{
  "name": "vehicles",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "vehicles/src",
  "prefix": "cw",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "vehicles/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
